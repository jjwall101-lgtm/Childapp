TEMPORARY TESTING RULES ONLY - do not use these for a public app.

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /claraAppSync/{familyId} {
      allow read, write: if true;
      match /{document=**} {
        allow read, write: if true;
      }
    }
  }
}

Use this only while testing Clara's private app.
For a public app, this must be replaced with proper authentication-based rules.
