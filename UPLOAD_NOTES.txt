IMPORTANT

I have updated script.js so it points to the new Firebase project:

projectId: childapp-af257

I also changed the internal localStorage keys and Firestore family record ID so this copy does not clash with Cameron's app if both are opened from the same GitHub Pages domain.

Missing from the uploaded files:
- style.css

Your current GitHub repo already shows style.css, so if you upload this package over the existing repo, do not delete the existing style.css file.
If you want a complete standalone ZIP, send me style.css as well and I can rebuild the package with it included.

I did not include the old file named "Cameron" because it is a separate old standalone HTML file and not needed for this version.
