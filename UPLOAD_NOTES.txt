CLARA V57 BUNNY TIMER + CACHE UPDATE

Upload these files to GitHub over the current Clara app:
- index.html
- style.css
- script.js
- manifest.json
- sw.js
- firestore.rules
- README.md
- FIREBASE_SETUP.md
- UPLOAD_NOTES.txt

Do not delete your existing icon.png or icon-192.png files from GitHub.

What changed:
- Added a new Timer tab.
- Timer matches the Bunny theme.
- Timer presets: 1, 2, 5, 10, 15 and 30 minutes.
- Parent Mode can set a custom timer label and duration.
- Timer state is saved in the shared Firebase family record.
- Cache version bumped to clara-v57-bunny-timer-cache-20260618.
- New sw.js deletes old caches and forces fresh HTML/CSS/JS/manifest loads.

After upload:
1. Open the GitHub Pages app link in Chrome.
2. Refresh once.
3. Close and reopen the app.
4. Anyone using the app from the home screen may need to open the browser link once so the service worker updates.
