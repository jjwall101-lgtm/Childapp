IMPORTANT

I have updated script.js so it points to the new Firebase project:

projectId: childapp-af257

I also changed the internal localStorage keys and Firestore family record ID so this copy does not clash with Clara's app if both are opened from the same GitHub Pages domain.

Missing from the uploaded files:
- style.css

Your current GitHub repo already shows style.css, so if you upload this package over the existing repo, do not delete the existing style.css file.
If you want a complete standalone ZIP, send me style.css as well and I can rebuild the package with it included.

I did not include the old file named "Clara" because it is a separate old standalone HTML file and not needed for this version.


BUNNY THEME UPDATE
- Theme is now forced to Bunny.
- Theme switcher is hidden.
- Bunny visual overrides are added inline in index.html, so the existing style.css in your repo can stay as it is.


CLARA COMPLETE UPDATE
- Replaced remaining visible Cameron references with Clara.
- Kept Firebase pointing to childapp-af257.
- Set Clara-specific local storage keys and Firestore family record ID.
- Bunny theme remains forced, with no theme selector shown.


CARROTS / DAY LABEL UPDATE
- Bunny slider added to the child screen.
- Coins are shown as carrots.
- Green/Amber/Red wording is shown as Amazing/Okay/Bad day.
- Internal values still use green/amber/red so existing behaviour logic continues to work.


FIXED PACKAGE
- Restored carrots wording.
- Restored child bunny slider.
- Kept the animated pink bunny footer.
- Bumped file versions to v=50 to help stop old cached files showing.
